export interface ProviderRateRow {
  providerName: string;
  vehicleType: string;
  chargingType: string;
  rate: number;
}

export interface ProviderRateCsvError {
  row: number;
  message: string;
}
export interface ProviderRateRow {
  providerName: string;
  vehicleType: string;
  chargingType: string;
  rate: number;
}

export interface ProviderRateCsvError {
  row: number;
  message: string;
}
